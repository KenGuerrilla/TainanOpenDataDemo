<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_main_graph"
    app:startDestination="@id/mainFragment">

    <fragment
        android:id="@+id/mainFragment"
        android:name="com.itl.kg.app.tainanopendatademo.MainFragment"
        android:label="fragment_main"
        tools:layout="@layout/fragment_main" >
        <action
            android:id="@+id/action_mainFragment_to_parkingActivity"
            app:destination="@id/parkingActivity" />
    </fragment>
    <activity
        android:id="@+id/parkingActivity"
        android:name="com.itl.kg.app.tainanopendatademo.ui.ParkingActivity"
        android:label="activity_parking"
        tools:layout="@layout/activity_parking" />
</navigation>